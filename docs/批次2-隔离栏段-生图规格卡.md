# 批次 2 · 隔离栏段分层重做 · 生图规格卡 v2.1

> （直接继承批次 1 v2 canonical 模板——绿幕底/比例与尺寸分离/400% 粗筛/物件三查/风格链一处定义/落位契约写死，全部范式照搬；本卡所有坐标已用 python 对 `chapters.json` 实测验证）
> （v2.1 审查修订已吸收：引擎渲染过滤器契约空洞 P1 / 16:9→2:1 裁切漂移 P1 / col59 栏线收口 / 岗哨改 3×4 奇数宽 / 警示牌挂板形态 / E2E 断言补缺。修订后契约已 python 重验：803 开格 0 死区、门/三热区/岗哨/残骸全连通）
> 用法：每张卡的**提示词直接复制**进生图工具，按编号顺序做（①是风格基准）。
> 生成完放进 `art/raw/`，按卡上的**文件名**命名，跟我说"放好了"——量化/抠绿/校验/集成/碰撞/测试/截图证据全是我的活。
> 本图叙事核心：**"看得见，进不去。"** 雾墙画进地貌、隔离栏拦在身前——所有设计都为这一句话服务。

## 共同硬规则（每张都要）

1. **背景一律纯绿幕**：物件卡（②-⑥）的背景必须是**一整片纯绿色**（提示词已写好 solid pure green #00FF00）。你的工具出不了透明 PNG，绿幕是这个项目一直在用的老办法（我负责抠绿）。**别要求透明底，别用棋盘格底**。
2. **工具里选比例，分辨率不用管**：每张卡写了"工具里选什么比例"。生成出来是 1024 还是 2048 都行，**最终尺寸我来缩**。
3. **30 秒像素粗筛**：出图后放大到 400% 看边缘——合格 = 一格一格的硬台阶；不合格 = 模糊渐变、毛玻璃边。**拿不准就直接给我，我跑行程统计脚本验**，不合格我会指出重 roll 哪张。
4. **物件卡（②-⑥）统一三查**：① 背景是一整片纯绿、绿色没混进物体本体 ② 画面里只有这一个物件——没有地面、没有影子 ③ 400% 看边缘是硬台阶。
5. 整体冷蓝灰调；**全批次禁琥珀禁暖棕**（琥珀是凤翎专属的全画面唯一暖色）。病态黄绿 #b9c46a **只允许出现在 ① 地貌卡的雾区内部、占比 <5%**（这里距灰域 0km，是全项目唯一许它露头的图）；②-⑥ 物件卡照旧全禁。建筑/设备灯光一律青绿 #6fd3c4。

---

## ① 地貌层 L0 · 灰域边缘荒地 + 雾墙（风格基准，先做这张）

| 项 | 值 |
|---|---|
| 文件名 | `edge_l0_terrain.png` |
| 工具里选 | **16:9 横版**（没有 2:1 选项就选最宽横版，**裁成 2:1 我来**） |
| 最终入库尺寸（我来缩+裁） | 960×480（60×30 格） |

```
pixel art, top-down view, 16-bit retro game terrain map, split horizontally into two zones: the LOWER 60% is a dead frontier wasteland at the very edge of an anomalous zone — cracked dark concrete, cold desaturated grey-blue dirt, sparse dead grey grass, faint old patrol tracks, more barren and lifeless than an ordinary buffer zone; the UPPER 40% is a massive wall of dense opaque grey fog rolling like a frozen tide, the fog is part of the terrain itself, an impenetrable soft-grey mass that swallows all detail, with a very faint sickly yellow-green (#b9c46a) tint deep inside the fog only (less than 5% of the image), a narrow scorched dead strip of ground where the fog meets the wasteland,
strictly no warm browns, no earth tones, cold grey-blue only,
true 1:1 pixel density, no anti-aliasing, no upscaling,
GROUND AND FOG ONLY — absolutely no buildings, no fences, no barriers, no structures, no characters, no props, no vehicles,
strict limited palette: dark blue-grey tones (#06080c #0f131c #2b333f #3a4452), pale grey fog, subtle texture variation only, cold dim Signalis mood, overcast lighting, no strong shadows
```

⚠️ 检查点：① **雾是地貌的一部分**——它要像"世界没写完"，不是一朵云、不是烟雾特效 ② **画面里不许有隔离栏**（栏是 L1 物件，画进去就重 roll）③ 病态黄绿只许在雾的深处隐约透出，下半荒地一个绿点都不许有 ④ 一栋建筑都不能有 ⑤ 雾与地的交界带要"死寂"（焦黑/枯灰条带），那是叙事重心所在的水平线 ⑥（我裁切时执行，你不用管）16:9→2:1 要裁掉约 11% 高度，**裁切窗以雾/地交界为基准对齐**——见集成清单的「裁后对齐验证」铁条；交界对不上时改裁切窗重裁，**严禁反向去改 rows0-12 行锁迁就图**。

---

## ② 物件 L1 · 隔离栏段（可平铺，横向重复 15 次——第 15 段是 col59 收口端段，只露 1 列入画）

| 项 | 值 |
|---|---|
| 文件名 | `edge_obj_fence.png` |
| 工具里选 | **2:1 或最宽横版**，主体居中、上下多留绿幕边 |
| 最终入库尺寸（我来缩） | 64×32（4×2 格） |

```
pixel art, top-down slightly angled view, 16-bit retro game fence sprite, one segment of a tall industrial quarantine barrier fence: heavy dark blue-grey metal pylons with dense reinforced mesh between them, small dim cyan (#6fd3c4) hazard lights along the top rail, weathered and stained but unbroken, the segment design is HORIZONTALLY SEAMLESS — left edge and right edge must connect perfectly when the segment repeats side by side,
true 1:1 pixel density, no anti-aliasing,
solid pure green background (#00FF00), background must be one flat green color, single isolated fence segment only, no ground, no baked shadow, no characters,
strict limited palette: dark blue-grey metal, cyan lights only, no warm colors
```

⚠️ 本卡特有检查点：① **左右边缘必须能无缝对接**——这一段会横向复制 15 次拼成整条栏，左右边缘图案对不上就会出现接缝（粗验法：把图复制一份左右并排看接缝处）。无缝还有第二个用途：第 15 段落 [59,13] 只露最左 1 列入画做画面收口，左缘接不上 [55,13] 段的右缘就会在画面最右破口 ② 栏要"高、密、不可翻越"——它是"进不去"三个字的实体 ③ 顶部青绿警示灯克制点缀，别做成霓虹。

---

## ③ 物件 L1 · 7号段维护闸门（通往 ch2，嵌在栏线正中）

| 项 | 值 |
|---|---|
| 文件名 | `edge_obj_gate.png` |
| 工具里选 | **3:2 或 4:3 横版**，主体居中、四周多留绿幕边 |
| 最终入库尺寸（我来缩） | 48×32（3×2 格，**宽 3 格=奇数，门正中对齐单格**） |

```
pixel art, top-down slightly angled view, 16-bit retro game gate sprite, a reinforced maintenance gate set into a quarantine barrier fence: two heavy dark blue-grey metal posts framing a closed sliding door at the bottom center about 26 pixels tall, a single cyan (#6fd3c4) status lamp above the door, small utilitarian signage plate, same construction family as an industrial mesh fence, sturdy and official,
true 1:1 pixel density, no anti-aliasing, gate door 24-32 pixels tall (scale anchor),
solid pure green background (#00FF00), background must be one flat green color, single isolated gate structure only, no ground, no baked shadow, no characters,
strict limited palette: dark blue-grey metal, cyan lights only, no warm colors
```

⚠️ 本卡特有检查点：① 门在**底边正中**，闸门两侧的柱体要能和 ② 栏段自然衔接（同族材质同族灯）② 这是全图唯一"开口"——要让人一眼读出"只有这里能过"。

---

## ④ 物件 L1 · 隔离栏警示牌（挂栏上，现有交互点原位升级）

| 项 | 值 |
|---|---|
| 文件名 | `edge_obj_sign.png` |
| 工具里选 | **3:2 横版**，主体居中、四周多留绿幕边 |
| 最终入库尺寸（我来缩） | 48×32（3×2 格，与现有 fence_sign 热区 [3,2] 一格不差） |

```
pixel art, top-down slightly angled view, 16-bit retro game sign sprite, an official quarantine warning panel designed to mount flat on an industrial mesh fence — panel and small mounting brackets only, no ground posts, no poles: a rectangular dark blue-grey panel with a cyan (#6fd3c4) backlit border and rows of small unreadable pixel glyphs suggesting regulation text, one hazard-stripe band along the bottom edge in desaturated grey tones, cold bureaucratic and intimidating,
true 1:1 pixel density, no anti-aliasing,
solid pure green background (#00FF00), background must be one flat green color, single isolated sign only, no ground, no baked shadow, no characters,
strict limited palette: dark blue-grey metal, cyan light only, no warm colors, no red
```

⚠️ 本卡特有检查点：① 牌面文字必须是**读不出来的像素符号**（真文字会糊成噪声）② 警示色用青绿和灰条纹，**禁红禁黄**——条文的冷漠感靠"官方青绿"传达（游戏里按空格弹出的才是真条文）③ **必须是挂板形态、不许有插地立柱**——这张牌渲染时直接盖在栏体之前（[33,13] 与栏段同占 rows13-14），带立柱的牌叠上去会像插进栏身/悬空，柱脚和栏网像素打架；出图见立柱直接重 roll。

---

## ⑤⑥ 可选 · 氛围散件（时间紧可跳过，不影响集成）

| 项 | ⑤ 废弃岗哨 | ⑥ 倒伏栏段残骸 |
|---|---|---|
| 文件名 | `edge_obj_sentry.png` | `edge_obj_debris.png` |
| 工具里选 | 3:4 竖版（没有就 1:1，左右多留绿幕边，裁 3:4 我来） | 3:1 或最宽横版（主体居中留边） |
| 最终入库尺寸 | 48×64（**3×4 格，宽 3=奇数，门正中对齐单格**） | 48×16（3×1 格） |

```
pixel art, top-down slightly angled view, 16-bit retro game building sprite, a small abandoned sentry post: boxy dark blue-grey metal hut with a flat roof, one boarded-up door at the bottom center about 24 pixels tall, dead dark windows, a broken antenna stub, long out of service with stains and dents, no lights at all,
true 1:1 pixel density, no anti-aliasing, boarded door 24-32 pixels tall (scale anchor),
solid pure green background (#00FF00), background must be one flat green color, single isolated building only, no ground, no baked shadow, no characters,
strict limited palette: dark blue-grey body only, no lights, no warm colors
```
```
pixel art, top-down slightly angled view, 16-bit retro game prop sprite, a collapsed fragment of industrial quarantine fence lying flat: bent dark blue-grey metal posts and torn mesh, rusted-grey not rusted-brown, defeated and forgotten, solid pure green background (#00FF00), single isolated prop, no ground, no baked shadow, true 1:1 pixel density, no anti-aliasing, no warm colors
```

⚠️ ⑤ 特有检查点：岗哨是**死的**——一个灯都不许亮（全图青绿灯只属于还在执勤的栏和闸门，明暗对比就是叙事）；宽 3 格=奇数遵守"带门建筑优先奇数宽"硬约束（封死的门不承担通行，纯装饰；若日后升级为可进设施，门格按铁律=[47,22]，中柱 col47，不必返工）。⑥ 残骸的"锈"必须是冷灰色调的锈蚀感，不许出现暖棕。

---

## 生成顺序与风格链（一处定义，照这个做）

1. **① 地貌**先做。**生成 ① 时把批次 1 的 `belt_l0_terrain.png` 当风格参考**——两图是同族地貌（缓冲带荒地），这张只是更荒、更死、雾占了上半（没有批次 1 成品就先做批次 1 的 ①，或独立生成后由我比对色板）
2. **② 栏段调到满意 → ③ 闸门、④ 警示牌都用 ② 当参考**（同族栏体构件优先于地貌）；**⑤ ⑥ 用 ① 当参考**
3. 用工具的**风格参考 / sref 模式**，别用垫图/重绘模式
4. ⚠️ 参考图只学色调——如果生成结果里**长出了地面**或**整体发绿**，说明参考权重太高，调低或去掉参考重 roll

---

## 落位契约（集成时我照此执行，门/出生点/三个交互热区一格不动——已用 python 对 chapters.json 实测验证，v2.1 修订后重验全过）

| 物件 | pos | 占格 | footprint（碰撞） | movable | 对应大门/交互 |
|---|---|---|---|---|---|
| 栏段 ② ×7（左） | [0,13] [4,13] [8,13] [12,13] [16,13] [20,13] [24,13] | 各 4×2，rows13-14 | 整框 2 行（栏=屏障） | **false**（嵌墙环境结构，不可拖） | — |
| 维护闸门 ③ | [28,13] | cols28-30 / rows13-14 | 整框 2 行 | true | **[29,15]→ch2 不动**，正对门中柱（宽 3 奇数，中柱=col29）✓ |
| 栏段 ② ×8（右） | [31,13] [35,13] [39,13] [43,13] [47,13] [51,13] [55,13] **[59,13]** | 各 4×2，rows13-14（**[59,13]=收口端段**：cols59-62 仅 col59 入画，出界 3 列画布自然裁掉） | 整框 2 行（端段出界列 clamp，col59 本就是边界墙、walkable 不变） | **false** | — |
| 警示牌 ④ | [33,13]（**现有热区原位**） | 3×2，rows13-14 | 无新增（isObstacle:false 保持，栏段已封该格） | **false** | 交互点（interactText 不动），渲染在栏段之后=盖在栏上（挂板形态，无立柱） |
| 废弃岗哨 ⑤ | [46,18] | **3×4，cols46-48 / rows18-21** | 底 2 行（**rows20-21，cols46-48**），上 2 行=屋顶只遮挡（y-sort 走身后） | true | 可选交互点（封死门中柱=col47） |
| 残骸 ⑥ | [12,20] | 3×1，row20 | 1 行 | true | — |
| 雾墙热区 ×2 | [10,12] / [44,12]（**数据一字不动**） | 各 6×3，rows12-14 | 无（isObstacle:false，noSprite/fromBg 热区） | false | 站 row15 按空格触发（与现状同几何） |

**铁律：门格 [29,15] = 闸门 footprint 最底行（14）的下一行、列对齐画面门正中。** 左段尾 col27 / 闸门 28-30 / 右段头 col31 严丝合缝，rows13-14 **cols0-59 视觉+碰撞双无泄漏**（col59 视觉由端段 ⑮ 收口、碰撞本就是边界墙——相机贴右边界时 col59 可见，不收口就是 16px 裸地断口）✓。出生点 [29,24] 不被任何 footprint 压 ✓。BFS（python 重验）：出生点→大门 / 雾墙西·东 / 警示牌 / 岗哨 / 残骸全连通，荒地区 **803 开格 0 死区**（岗哨 4×4→3×4 释放 2 格）✓。

⚠️ **movable:false + sprite 的渲染契约（本卡集成的前置改造，不许漏）**：`index.html` bgImage 房间的物件渲染过滤器当前是 `p.movable && p.sprite`——按本卡落位，15 段栏 + 警示牌都是 movable:false + sprite，**不改过滤器它们一个都画不出来 = 整条隔离栏隐形**。集成第一步先把过滤器改成 `p.sprite`（已 python 核验：bgImage 房间 ch4/area_city/area_belt/area_edge 现无任何 movable:false+sprite 存量 = 零回归；ch2 的 3 个同类物件走 `!room.bgImage` tile 分支、不经此过滤器，不受影响）。栏/牌保持 movable:false 与 ch4「嵌墙环境结构」教义一致——**故意不给拖**：saveChapter 的 BFS 防呆只验出生点→大门连通，创作者若能拖走一段栏就能在 rows13-14 开口而机器不报警。编辑器侧 fixed+sprite 本就显示贴图（只可点选改属性），无需改。

⚠️ **雾区行手工封锁（本图特有铁条，不许漏）**：新 walkable 必须把 **rows0-12 整行写死为 1**——"地貌默认全可走"在这张图的上半**不成立**，雾区+栏以北永不可进是叙事核心。栏 footprint（rows13-14）只补栏线，上面 13 行靠这条手工锁。

## 我这边的集成清单（你不用管，备忘）

- [ ] **【铁条·先行】引擎渲染过滤器改造**：index.html bgImage 房物件渲染 filter 由 `p.movable && p.sprite` 改为 `p.sprite`（详见落位契约下方的渲染契约段——不改则 15 段栏+警示牌全部隐形；已核零回归）
- [ ] **【铁条】① 地貌 16:9→2:1 裁后对齐验证**：裁切窗以**雾/地交界为基准**——交界行必须落在 **row12±1**（最终 960×480 图上 y≈192-208px 带内）；入库后用像素扫描验证（仿 `art/verify_bgmode.py`：扫雾色带的下边界行号），不过就**改裁切窗重裁**，**严禁反向去改 rows0-12 行锁迁就图**（交界下漂过 row14=雾盖在可走 row15 上、能"走进雾"毁叙事；上漂=大片可见净地被锁死、肉眼空气墙——正是分层 v2 要消灭的 bug 类型）
- [ ] 抠绿脚本 `art/chromakey.py`（与批次 1 共用，批次 1 先建则直接复用）
- [ ] 入库校验三连：抠绿后 alpha 干净（青绿灯别误伤）/ 32 色量化 / 行程统计验真像素；② 栏段加一项**左右并排接缝检查**
- [ ] walkable 重生成：四周 1 圈边界墙 + **rows0-12 雾区整行手工封锁** + 栏段/闸门/岗哨/残骸 footprint 写 1（沿 gen_areas.py 幂等范式）；**端段 [59,13] 出界列（cols60-62）必须 clamp**——gen 脚本与编辑器 `applyPropObstacle` 写格前都要核出界保护
- [ ] fence_sign 数据迁移：fromBg:true→false + 挂 sprite key（pos/size/interactText/isObstacle/**movable:false** 一字不动——可见性由上面的过滤器改造保证）；fogwall_w/e **完全不动**
- [ ] 栏段 props 落位：**15 个** prop 共用同一 sprite key、id 取 fence_seg_01..15（第 15 段=col59 收口端段）、**全部 movable:false**；闸门/岗哨/残骸各 1 个新 prop（movable:true + footprint + isObstacle）
- [ ] 渲染顺序：警示牌在栏段之后 add（同底行 y-sort 平局时后 add 在上，牌要盖在栏前）
- [ ] BootScene preload 注册新贴图 key（edge_l0_terrain + 各物件 sprite）
- [ ] area_edge 改造：room.bgImage 换 edge_l0_terrain + 按落位契约摆件 + 大门/出生点/三热区保持现值
- [ ] **E2E 补缺再跑全量**（诚实口径：现有 61 项里 AREA 组只验维度/底图/BFS 连通，**雾墙/警示牌空格触发目前无自动化承载**，rows0-12 行锁也无机器断言——集成时给 `test/e2e_smoke.py` 补三条再跑）：① INTERACT 组加 `?room=area_edge&simact=fogwall_w`（`?room=` 是 debug 直入、可绕过 locked，探针模式现成）② AREA 组对 area_edge 断言 **walkable rows0-12 全为 '1'**（防编辑器笔刷/未来重生成意外打开雾区）③ 承接批次 1 承诺的 **isObstacle 物件 footprint 格在 walkable 必须=1**（防幽灵墙，数据驱动、三区通用）
- [ ] 世界图缩略图：旧 `graymist_barrier.png` 不覆盖（暂留作 thumb），终态用实景截图换
- [ ] y-sort 遮挡截图证据：调查员走到岗哨身后被屋顶盖住（markdown 嵌图交付）
